# Learning

## Data Notes

- On 2026-08-14, Eastmoney `ak.fund_etf_spot_em` / `fund_etf_hist_em` and `push2` ETF list pages often reset. Industry ETF daily bars should use Tencent `fqkline` qfq with yearly chunks (`amv_index_data.download_index_daily`). Years before an ETF listing come back empty and must be skipped, not treated as a hard failure. Cached under `data/backtests/amv_index_gate/index_{tencent_symbol}_daily.csv`.
- On 2026-08-14, 0AMV close for 2026-08-14 was entered as the same 207502.5 as 2026-08-13 (`ret=0`). Do not use that bar as a live AMV signal until a distinct close is confirmed. Sector-ETF research therefore ends 2026-08-13.
- On 2026-08-14 evening, local Compass `day.vdat` showed the 08-13 close was actually **208572.30**, not 207502.5. The 207502.5 value was 08-14's real close (OHLC 209709.80 / 211843 / 206864.20 / 207502.50, ret -0.51%). Manual/GitHub fills had overwritten 08-13. `seed_cloud_amv_from_local` must prefer Compass bars on the same date (stable sort, compass source rank), otherwise pandas `drop_duplicates` can keep the wrong fill and make 08-14 look like a duplicate.
- On 2026-08-14, Tonghuashun daily breadth `all_a_breadth_ths_2022_present.csv` was already current through 2026-08-14, but `all_a_breadth_combined.csv` had stopped at 2026-07-17. Rebuilding combined (now also written by `load_complete_history(mode="close")`) is required after THS refresh. Latest emotion 2026-08-14 = 43.3; seven sessions from 2026-07-20 to 2026-08-13 had emotion ≥70.
- On 2026-08-14, Lightweight Charts default `minBarSpacing` (~6px) cannot fit a multi-year daily NAV on screen, so the public site looked zoomed-in and required panning. Use `minBarSpacing: 0.05` plus `timeScale().fitContent()` for the default 成立以来 view.
- On 2026-08-14, public Pages site cannot run Compass/TongDaXin. Persist sanitized 0AMV as `data/amv/0amv_daily.csv`. GitHub Actions cron `0 12 * * 1-5` (20:00 CST) refreshes Tencent index/ETF overlays even when 0AMV is stale; banner then says the clock reuses the last distinct close. Duplicate 0AMV close (08-14 copied 08-13's 207502.5) must not be treated as a live signal. Tencent/THS fetch on Actions needs `curl` not `curl.exe`.
- On 2026-08-14, TooColdCC 韭菜50 public feed is `https://indices-toocoldcc.pages.dev/data/agent/bagholder50.json`. After 15:00 Beijing, `auto` view should be next-session `avoid_tomorrow`; constituents are public. Other TooColdCC indices (composite/growth/value/cam) constituents are subscriber-only and must not be inferred.
- On 2026-08-14, Tonghuashun fund-flow period symbols are `即时` / `3日排行` / `5日排行` / `10日排行` / `20日排行`, not `3日`. Non-instant tables use `阶段涨跌幅` and have no 领涨股. Public site merges those windows onto `data/site/theme_fund_flow.csv`.
- On 2026-08-14, `sector_etf_rotation.rotate()` needs the official backtest daily (`run_index_backtest` `action` column), not `apply_combined_rule` which only has `entry_signal`/`exit_signal`. Gate: 创业板有仓才选板块. Public overlay is monthly 120d top-2 equal-weight, not daily top-1.

- On 2026-06-16, common TongDaXin paths were checked and were not found.
- AKShare is the MVP default source because it covers K-line and board data.
- The first sector universe should exclude traditional industries unless they are directly tied to the selected technology themes.
- On 2026-06-16, Eastmoney `push2` board endpoints disconnected repeatedly in this environment. AKShare TongHuaShun board endpoints worked and were set as the default technology board source.
- On 2026-06-16, technology board data was downloaded to `data/tech_boards/tech_boards_20260616_141058`: 28 concept boards, 3 industry boards, 31 history files, and 31 info files.
- On 2026-07-18, TongDaXin `MAINZSHQ(M,6/7)` was observed to return the current breadth snapshot across the whole chart, producing a horizontal historical line; do not use it for daily historical backtests. Historical multi-market breadth requires `CALCSTOCKINDEX` plus an installed `QXBASE` helper formula whose first two outputs are `ADVANCE` and `DECLINE`.
- On 2026-07-24, unattended refresh = launch `WavMain.exe` with saved auto-login + poll `day.vdat`; there is no headless official downloader.
- On 2026-08-03, passive launch alone did not advance 0AMV (`day.vdat` stuck at 2026-07-31). Working UI path: toolbar **下载** → dialog **数据接收** → **接收最新(推荐)** → **开始**. Do **not** auto-click **全选** (can become ~146k-package full history). While downloading, `day.vdat` may raise `PermissionError`; wait for unlock. Module: `compass_ui.py`, wired into `sync_and_export(ui_download=True)`.
- On 2026-08-06, aggressive UI scan (many SetCursorPos/SetForegroundWindow) + relaunch loops disrupted the desktop. Hardened sync: PostMessage toolbar clicks (no cursor teleport), ≤2 open attempts, sync lock file skips overlapping jobs, intraday bat uses `--no-launch --keep-open`, failure fallback uses `--no-launch --allow-stale` without second relaunch. Also added Tk popup manual 0AMV close input (`compass_manual_input.py`, `--no-prompt-manual` to disable; env `CYB_AMV_MANUAL_CLOSE` for tests).
- On 2026-08-06, sync order changed to light-first: type `0AMV` (`browse_0amv_symbol`) → wait → only then 下载→接收最新 → then manual popup. Flags: `--no-ui-browse`, `--browse-wait`. Typing alone may refresh when auto day-fill is on; after-hours day.vdat still often needs download (see 2026-08-03).
- On 2026-08-10, daily bats switched off Compass launch: `prompt_0amv_ret.py` asks for today's 0AMV **close** (derive ret% from prior CSV bar). Strategy uses `amv_ret_1d` / `amv_ret_2d_sum`. Env: `CYB_AMV_MANUAL_CLOSE` (optional `CYB_AMV_MANUAL_RET_PCT` fallback).
- On 2026-07-24, 0AMV gate backtest (`amv_index_gate`) uses Tencent fqkline yearly chunks for five indices; close-confirm / next-open fill; among 2019–2026 samples only CSI1000 beat buy-and-hold on total return under 10bp costs, while ChiNext had highest absolute strategy return but large negative excess vs B&H.
- On 2026-07-24, underperformance vs B&H was mainly ~46% exposure despite positive timing. OOS grid winner `exit_protect_idx_ma20` ignores AMV crash exits while index close > MA20; CSI1000 full-sample excess ~+49%, STAR50 turns positive; ChiNext still lags B&H. Blindly raising exposure toward always-in hurt OOS excess.
- On 2026-07-24, fair compare on ChiNext ETF overlap (emotion∩0AMV, next-open, 10bp): classic emotion+KDJ lags badly vs `amv_min10_protect_ma20`. Strict emotion∧AMV entry almost never fires; all AND-grid rules have negative IS excess. Emotion helps more as hot-exit filter (emotion≥60–70) on AMV entries than as co-entry. OR+emotion-exit grid tops look spectacular but trade-dense and overfitting-prone—treat as research ceiling only.
- On 2026-07-24, robust optimize (~100 hypothesis rules, WF 2022–2025, cost stress, 2026 holdout): extra overlays (trend/ATR%ile/vol/RSI/ATR trail) rarely beat a lean AMV+emotion+MA60 skeleton after complexity penalty. Score winner: ain0.03 + (amv≤-3.5% or emotion≥60) + MA60 protect. More stable pick: `amv_emo70_ma60` (100% positive WF folds, better 2026 holdout). Prefer small threshold tweaks over stacking indicators.
- On 2026-07-24, daily QQ email monitor switched from classic emotion+KDJ to live score-winner rule (`amv_emo60_aout35_ma60`); state file now `data/monitor/cyb_amv_emotion_strategy_state.json`; requires fresh Compass 0AMV CSV.
- On 2026-08-14, consolidated scheduled tasks to one weekday job at 15:30 (`CYB Signal Monitor Close 15-30`): prompt 0AMV close + close email only. Removed 14:40 intraday, 15:35 sync, 15:45 close tasks.
